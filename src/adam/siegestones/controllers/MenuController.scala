package adam.siegestones.controllers

import java.net.URL;
import java.util.ResourceBundle;

import javafx.collections.FXCollections;
import javafx.collections.ObservableList;
import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.fxml.Initializable;
import javafx.scene.control.Button;
import javafx.scene.control.ComboBox;
import javafx.scene.control.TextField;

class MenuController extends Controller with Initializable {

  @FXML
  private def onStartButtonAction(event: ActionEvent) {
  }

  @FXML
  private def onPlayerTypeComboBoxAAction(event: ActionEvent) {
  }

  @FXML
  private def onPlayerTypeComboBoxBAction(event: ActionEvent) {
  }

  override def initialize(arg0: URL, arg1: ResourceBundle) {}
}